import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

  container: {
    flex: 1,
    backgroundColor: '#fff'
  },

  img: {
   height: 300,
   width: 300,
  },
  
  img_container: {
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 70,
  },

  activity: {
    marginTop: 50
  }

})